{
  "name": "gulp-task-constructor",
  "version": "0.0.2",
  "main": "./target/index.js",
  "types": "./target/index.d.ts",
  "author": "Markus Spanier <markus.spanier@tngtech.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/marspa/gulp-task-constructor.git"
  },
  "scripts": {
    "build": "yarn run ci && yarn run compile",
    "ci": "yarn run lint && yarn run test",
    "compile": "rimraf target && tsc",
    "clean": "yarn run clean:coverage && rimraf node_modules target",
    "clean:coverage": "rimraf coverage .nyc_output",
    "lint": "tslint src/**/*.ts test/**/*.ts",
    "test": "yarn run clean:coverage && nyc mocha --opts mocha.opts",
    "test:watch": "yarn test -- --watch"
  },
  "nyc": {
    "include": [
      "src/**/*.ts"
    ],
    "extension": [
      ".ts"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [
      "text-summary",
      "html"
    ]
  },
  "dependencies": {
    "gulp": "^3.9.1",
    "inversify": "^3.1.0",
    "reflect-metadata": "^0.1.9",
    "run-sequence": "^1.2.2",
    "uuid": "^3.0.1"
  },
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/gulp": "^3.8.32",
    "@types/mocha": "^2.2.35",
    "@types/node": "^6.0.55",
    "@types/run-sequence": "^0.0.28",
    "@types/sinon": "^1.16.34",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "mocha-sinon": "^1.1.6",
    "nyc": "^10.0.0",
    "rimraf": "^2.5.4",
    "sinon": "^1.17.7",
    "ts-node": "^2.0.0",
    "tslint": "^4.2.0",
    "typescript": "^2.1.4"
  }
}
